<h2><%= @patient.name %></h2>

<ol>
    <%= @patient.appointments.each do |pa| %>
        <li><%= pa.appointment_datetime.strftime("%B %d, %Y" + ' at ' "%H:%M") %></li>
        <li><%= link_to pa.doctor.name, pa.doctor %></li>
    <% end %>
</ol>